apply plugin: "findbugs"

String findbugsLocation = "$rootProject.projectDir/gradle/plugins/findbugs"

findbugs {
    sourceSets = [sourceSets.main]
    effort = "max"
    toolVersion  = "$findbugsVersion"
    reportLevel = "high"
    excludeFilter = file("$findbugsLocation/findBugsExclude.xml")
}

findbugsMain {
    reports {
        html.enabled = true
        xml.enabled = false
    }
}
